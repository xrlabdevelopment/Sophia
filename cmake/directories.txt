# -----------------
# Directories       
# ----------------- 
SET(SRC_DIR src)
SET(BIN_DIR bin)
SET(INSTALL_DIR install)

SET(OUTPUT_BINDIR "${PROJECT_BINARY_DIR}/${BIN_DIR}")
SET(UNITY_BINDIR "Editor/Data/Managed")

SET(CMAKE_INSTALL_PREFIX "C:/DAE" CACHE STRING "InstallPrefix" FORCE)